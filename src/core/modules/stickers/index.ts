import { Bot } from 'grammy'
import { Cortana } from '@context'
// import { kangCmd } from './kang';

export default function stickersModule(bot: Bot<Cortana>) {
  // bot.command('kang', async (ctx) => kangCmd(ctx));
}
