export interface IOwner {
    id: number;
    username: string;
    first_name: string;
    db: string;
};

export interface IBotConfig {
    id: number;
    username: string;
    first_name: string;
    repository: string;
}

